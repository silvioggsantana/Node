<h1>Handlebars</h1>
<p> teste de handlebars</p>
{{#if authenticated}}
    {{#each user}}
        {{> user }}
    {{/each}}
    {{#with userAdmin}}
        <p>Name: {{{name}}}</p>
        <p>Age: {{{age}}}</p>
        <p>Email: {{{email}}}</p>
    {{/with}}
{{else}}
    <h2>User not authenticated</h2>
{{/if}}

